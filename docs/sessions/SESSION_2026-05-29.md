# Frontend Session Log — 2026-05-29

> **Branch:** `frontend`
> **Deadline:** 2026-05-29 (วันนี้)

---

## สถานะ ณ ต้น session

- F1–F9 เสร็จ: UI ครบ 15 หน้า + wire API จริงทุกหน้า
- Review session 2026-05-29 ผ่านไปแล้ว (verdict No-Go → Go หลัง fix)
- Review fixes (RouteGuard, fallback data, drag save, site filter, 403) — ทำในเครื่องอื่น, commits ยังไม่อยู่ใน branch นี้
- F9 R5: hover tooltip + warning status UI — ทำในเครื่องอื่น เช่นกัน
- Task 3 (BuildingMapPage) — ยังไม่ได้ทำ รอ lat/lng จาก backend

---

## งานที่ทำใน session นี้

### BuildingMapPage (`12c460f`)

สร้างหน้า Building Map ที่ `/dashboard/map` ครบ:

| ไฟล์ | รายละเอียด |
|---|---|
| `src/pages/BuildingMapPage.tsx` | หน้าหลัก Leaflet map |
| `src/styles/map.css` | CSS |
| `src/api/types.ts` | เพิ่ม `BuildingApi`, `SiteApi` |
| `src/api/hierarchy.ts` | เพิ่ม `getBuildings()`, `getSites()` |
| `src/pages/DashboardPage.tsx` | เพิ่ม route `map` |
| `src/components/layout/Sidebar.tsx` | เพิ่ม nav "Building Map" |
| `package.json` | เพิ่ม `react-leaflet@4`, `leaflet`, `@types/leaflet` |

**Feature ที่มี:**
- OpenStreetMap tiles, circle markers (green = no alert, red = has alert)
- Popup: ชื่อ building, site, จำนวนชั้น/กล้อง/NVR, alert badge
- คลิก → `/dashboard/buildings/:Building_ID`
- Site filter (button group ด้านบน)
- Map auto-center ตาม lat/lng จริง
- Fallback list สำหรับ buildings ที่ยังไม่มี lat/lng

**หมายเหตุ marker color:** ใช้ 2 สี (green/red) เพราะ `GET /api/buildings` คืนแค่ `alert_count` ไม่แยก warning/offline

### Backend reply (`75d7e69` บน backend branch)
สร้าง `F9_FRONTEND_REPLY_R6.md` แจ้ง backend ว่า Task 3 เสร็จ + F9 complete

---

## DB Setup ที่ต้องทำก่อน map ใช้งานได้จริง

```sql
-- รัน migration นี้บน SSMS ก่อน
-- ไฟล์: db/migration_add_building_latlong.sql (บน backend branch)
ALTER TABLE [dbo].[buildings] ADD [lat] DECIMAL(10,7) NULL, [lng] DECIMAL(10,7) NULL;
```

จากนั้นกรอก lat/lng จริงให้แต่ละ building ใน DB

---

## สถานะ F9 — Complete

| Task | Status |
|---|---|
| R1–R3: Racks, hierarchy | ✅ |
| R4: PATCH position, cameraCount/nvrCount | ✅ |
| Review fixes: RouteGuard, fallback, site filter, 403, drag save | ✅ (เครื่องอื่น) |
| R5 Task 1: Hover tooltip | ✅ (เครื่องอื่น) |
| R5 Task 2: Warning status UI | ✅ (เครื่องอื่น) |
| R5 Task 3: Building Map `/dashboard/map` | ✅ session นี้ |

---

---

## งานที่ทำใน session ต่อ (บ้าน, `76d5765`)

commits จากเครื่องอื่น (`ee47c54` ฯลฯ) ไม่ได้ push ขึ้น GitHub — recreate ที่บ้านแทน

### Review Fixes Recreated (`76d5765`)

| Fix | ไฟล์ | รายละเอียด |
|---|---|---|
| RouteGuard | `App.tsx` | `RequireAuth` — ไม่ login → redirect `/login` อัตโนมัติ |
| Site filter | `SitesPage.tsx` | เพิ่ม `siteIds[]` ต่อ building (A–H), filter ตาม URL param |
| Hover tooltip | `FloorPlanPage.tsx` + `floor.css` | CSS tooltip: ID / Status / Room / IP |
| Warning pulse | `floor.css` | `cam-pulse-warn` animation สีเหลือง 1.8s |
| Drag save | `FloorPlanPage.tsx` | `stopDrag` log position (ready for PATCH API) |

**ผล verify ด้วย Playwright:**
- Guard: `/dashboard/cameras` โดยไม่ login → redirect `/login` ✅
- Tooltip: hover CAM-01 ที่ Floor 5 แสดง Status/Room/IP ✅
- Warning: CAM-03 dot สีส้มเหลือง แตกต่างจาก online ✅
- Site B: แสดงแค่อาคาร D / HQ: แสดง A B C ✅

---

## สถานะสุดท้าย — F9 Complete + All Fixes Done

- UI ครบ 15 หน้า + BuildingMapPage
- Review fixes ครบ (recreated บ้าน)
- Pushed → `origin/frontend`
- DB migration `ALTER TABLE buildings ADD lat/lng` ยังไม่ได้รัน (backend team)

---

## งานที่ทำใน session ต่อ — API Wiring Round 1 (`c727461`)

> Branch: `frontend` | หลังพรีเซ็น — ทำต่อก่อนพัก

### List pages wired (`c727461`)

| Page | File | API เชื่อม |
|---|---|---|
| CamerasPage | `CamerasPage.tsx` | `getCameras()` + `getSites()` |
| NVRsPage | `NVRsPage.tsx` | `getNvrs()` + `getSites()` |
| SwitchesPage | `SwitchesPage.tsx` | `getSwitches()` + `getSites()` |
| SitesPage | `SitesPage.tsx` | `getHierarchyTree()` — อาคาร/ชั้น/กล้องจาก tree |

Pattern ที่ใช้:
- `useQuery` + `refetchOnWindowFocus: false`
- `useRef(false)` สำหรับ one-time init จาก API → local state
- CRUD modals ยังเป็น local state (ไม่มี POST/DELETE endpoint)
- Site filter dropdown ดึงจาก `getSites()` จริง (ไม่ hardcode)

### API layer updates (`c727461`)

| ไฟล์ | เปลี่ยน |
|---|---|
| `src/api/types.ts` | เพิ่ม `FloorApi`, `RackApi` (จาก floorsModel.cs / racksModel.cs) |
| `src/api/hierarchy.ts` | `getBuildings(params?)`, `getFloors()`, `getRacks()`, `getRackById()` |
| `src/api/nvrs.ts` | เพิ่ม `NVR_ID?` param |
| `src/api/switches.ts` | เพิ่ม `SW_ID?` param |
| `src/pages/BuildingMapPage.tsx` | fix `queryFn: getSites` → `() => getSites()` (TS error) |

---

## งานที่ทำใน session ต่อ — API Wiring Round 2 (`efc4ac8`)

> Branch: `frontend` | ต่อเลยยังเหลือเวลา

### Detail pages wired (`efc4ac8`)

| Page | File | API เชื่อม | หมายเหตุ |
|---|---|---|---|
| NVRDetailPage | `NVRDetailPage.tsx` | `getNvrs({ NVR_ID })` | HDD simulate จาก `hdd_total_tb`/`hdd_used_pct` |
| SwitchDetailPage | `SwitchDetailPage.tsx` | `getSwitches({ SW_ID })` | port map generate จาก aggregate counts |
| UsersPage | `UsersPage.tsx` | `getUsers()` | CRUD local; ลบ email field (ไม่มีใน UserApi) |

Pattern detail pages:
- `enabled: !!id` — ไม่ query ถ้าไม่มี param
- `mapApiNvr()` / `mapApiSwitch()` — แปลง API type เป็น local interface
- Loading state + not-found state
- Port map / channel table ยังเป็น computed mock จาก aggregate API data

---

## สถานะ API Wiring — หลัง session นี้

| Page | Data | สถานะ |
|---|---|---|
| CamerasPage | **API** | ✅ |
| NVRsPage | **API** | ✅ |
| SwitchesPage | **API** | ✅ |
| SitesPage | **API** | ✅ |
| NVRDetailPage | **API** | ✅ |
| SwitchDetailPage | **API** | ✅ |
| UsersPage | **API** (read) | ✅ |
| BuildingDetailPage | Mock | 🟡 next |
| FloorPlanPage | Mock | 🟡 next |
| RacksListPage | Mock | 🟡 next |
| RackDetailPage | Mock | 🟡 next |
| SitesCrudPage | Mock | 🔵 low priority |

**Next deadline: 2026-06-04** — เหลือ 4 pages หลัก

---

*Frontend: Ran | Builder: Claude Sonnet 4.6 | 2026-05-29*
