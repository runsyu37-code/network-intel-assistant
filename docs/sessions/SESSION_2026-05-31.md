# Backend Session Log — 2026-05-31

> **Branch:** `backend` | **Machine:** Home PC (DESKTOP-OAVDR88)
> **Status:** Mock data seeded ✅ — API running ✅ — Login pending password reset

---

## งานที่ทำในวันนี้

| งาน | สถานะ |
|---|---|
| `PATCH /api/buildings/{Building_ID}/coordinates` | ✅ เพิ่ม endpoint ใหม่ |
| `mock_data.sql` — rewrite สำหรับ 4 buildings | ✅ seed เข้า DB สำเร็จ |
| `MOCK_DATA_GUIDE.md` — คู่มือ clean + seed | ✅ สร้างใหม่ |
| `migration_add_camera_position.sql` | ✅ รันบน SSMS แล้ว |
| Web.config — แก้ทุก issue (ดูด้านล่าง) | ✅ แก้ครบ |
| Mock data seed 1 site / 4 buildings / 11 floors / 12 cameras | ✅ |
| UQ_cam_nvr_ch — DROP constraint + simplify NVR_CH เป็นแค่ NVR ID | ✅ |

---

## Web.config fixes (home PC — gitignored ไม่กระทบเครื่องอื่น)

| ปัญหา | วิธีแก้ |
|---|---|
| `Microsoft.Web.Infrastructure` version mismatch (2.0→1.0) | แก้ binding redirect เป็น `1.0.0.0` |
| `System.Memory` version mismatch (BCrypt dependency) | เพิ่ม binding redirect `4.0.1.2` |
| Connection string ชี้ไปโปรเจกต์อื่น (`YieldSteps`) | เปลี่ยนเป็น `DESKTOP-OAVDR88\SQLEXPRESS;SSM_DB;Integrated Security=True` |
| `JwtSecret` หายจาก appSettings | เพิ่ม `SSM-Dev-Secret-2026-HomePC-DoNotCommit` |

---

## สถานะ DB (ณ session นี้)

| ตาราง | rows |
|---|---|
| sites | 1 (S001) |
| buildings | 4 (B001–B004) พร้อม lat/lng |
| floors | 11 |
| rooms | 4 |
| racks | 4 |
| poe_switches | 4 |
| nvrs | 4 |
| cameras | 12 (online 9 / warning 1 / offline 2) พร้อม position_x/y |
| ping_logs | 12 |
| alert_logs | 3 |

---

## ค้างอยู่ — ต้องทำต่อ

### 1. Reset admin password (ทำก่อนอื่น)

Password ที่ seed ไว้ใน DB hash ไม่ตรงกับ `Admin@SSM1` — ต้อง reset ด้วย query นี้:

```sql
-- gen hash ก่อนด้วย PowerShell (รันใน terminal):
-- [Reflection.Assembly]::LoadFile("C:\1_Work_Local\backend-latest\BNO_Survei_MonitorAPI\BNO_Survei_MonitorAPI\bin\BCrypt.Net-Next.dll")
-- [BCrypt.Net.BCrypt]::HashPassword("Admin@SSM1", 12)
-- แล้วเอา hash มาใส่ด้านล่าง

USE SSM_DB;
UPDATE [dbo].[users] SET pw_hash = '<hash จาก PowerShell>' WHERE username = 'admin';
UPDATE [dbo].[users] SET pw_hash = '<hash จาก PowerShell>' WHERE username = 'ssm_user';
```

### 2. FE error message หายเร็วเกินไป

ตอน login ผิด ข้อความแจ้งเตือนแดงหายภายใน 1-2 วิ — น่าจะเป็น `setTimeout` หรือ Ant Design notification duration สั้นเกิน ดูใน `LoginPage.tsx`

### 3. ทดสอบทุก page หลัง login ได้

ไล่ตามลำดับ: Dashboard → Topology → Map → Floor → Audit

---

## Commits session นี้

| Hash | งาน |
|---|---|
| `106580a` | feat(buildings): PATCH coordinates endpoint |
| `3d3b509` | docs(db): MOCK_DATA_GUIDE + rewrite mock_data.sql |
| `9c262e9` | fix(db): camera position migration + alert_type fix |
| `af55fde` | fix(db): prefix NVR_CH with NVR ID (unique constraint) |
| `b4eef32` | fix(db): simplify NVR_CH to NVR ID label only |

---

## ไฟล์สำคัญที่ต้องโหลดหลัง /clear

```
DEV.md                              ← status + Phase Gates
NEXT.md                             ← งานที่ค้างอยู่
docs/sessions/SESSION_2026-05-31.md ← ไฟล์นี้
db/MOCK_DATA_GUIDE.md               ← วิธี clean + seed DB
docs/sessions/CROSSMACHINE_FIX_2026-05-28.md ← แก้ Web.config issues
```

---

*Backend: Ran | Builder: Claude Sonnet 4.6 | 2026-05-31*
