info:
  name: LOGIN
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:50680/api/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: '{"username": "viewer_test", "password": "Test@1234"}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
