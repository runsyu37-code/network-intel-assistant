info:
  name: ME
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:50680/api/auth/me
  headers:
    - name: Authorization
      value: Bearer {{admin_token}}
      enabled: true


settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
