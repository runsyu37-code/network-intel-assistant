info:
  name: UPDATE
  type: http
  seq: 3

http:
  method: PUT
  url: https://localhost:44342/api/Updatesites/S001
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
      enabled: true
  body:
    mode: json
    json: |-
      {
        "Site_ID": "S001",
        "name": "สำนักงานใหญ่ (Updated)",
        "code": "HQ",
        "location": "กรุงเทพมหานคร",
        "description": "Updated via Bruno test"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
