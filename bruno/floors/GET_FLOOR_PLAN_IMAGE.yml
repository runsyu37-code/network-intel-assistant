info:
  name: GET_FLOOR_PLAN_IMAGE
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:50680/api/floors/F001/floor-plan/image
  headers:
    - name: Authorization
      value: Bearer {{admin_token}}
      enabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
