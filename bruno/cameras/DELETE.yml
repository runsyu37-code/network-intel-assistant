info:
  name: DELETE
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:50680/api/cameras/delete/4
  headers:
    - name: Authorization
      value: Bearer {{admin_token}}
      enabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
