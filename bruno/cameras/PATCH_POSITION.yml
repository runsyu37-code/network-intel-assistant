info:
  name: PATCH_POSITION
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:50680/api/cameras/1/position
  headers:
    - name: Content-Type
      value: application/json
      enabled: true
    - name: Authorization
      value: Bearer {{admin_token}}
      enabled: true
  body:
    mode: json
    json: |-
      {
        "x": 0.45,
        "y": 0.30
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
