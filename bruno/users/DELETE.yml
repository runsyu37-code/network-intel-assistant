info:
  name: DELETE
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:50680/api/users/delete/4
  auth: inherit
  headers:
    - name: Authorization
      value: Bearer {{admin_token}}
      enabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
