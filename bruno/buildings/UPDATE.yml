info:
  name: UPDATE
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:50680/api/buildings/B001
  auth: inherit
  headers:
    - name: Authorization
      value: Bearer {{admin_token}}
      enabled: true
    - name: Content-Type
      value: application/json
      enabled: true
  body:
    mode: json
    json: |-
      {
        "Building_ID": "B001",
        "Site_ID": "S001",
        "name": "à¸­à¸²à¸„à¸²à¸£ A (Updated)",
        "code": "BLD-A",
        "floor_count": 5,
        "description": "Updated via Bruno test",
        "image_data": null,
        "image_type": null,
        "note": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
