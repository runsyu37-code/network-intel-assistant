info:
  name: RBAC16 - viewer GET /api/racks → 403
  type: http
  seq: 16

http:
  method: GET
  url: http://localhost:50680/api/racks
  headers:
    - name: Authorization
      value: Bearer {{viewer_token}}
      enabled: true

runtime:
  variables:
    - name: viewer_token
      value: FILL_IN_VIEWER_TOKEN

# EXPECT: 403 Forbidden
# FAIL if: 200 (viewer can only see sites/buildings/floors/floor-plans)
