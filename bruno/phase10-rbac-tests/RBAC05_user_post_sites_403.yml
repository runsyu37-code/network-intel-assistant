info:
  name: RBAC05 - user POST /api/sites → 403 (write is admin-only)
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:50680/api/sites
  headers:
    - name: Authorization
      value: Bearer {{user_token}}
      enabled: true
    - name: Content-Type
      value: application/json
      enabled: true
  body:
    mode: json
    json: |-
      [{"Site_ID":"TEST-RBAC-USR","name":"User Role Test Site"}]

runtime:
  variables:
    - name: user_token
      value: FILL_IN_USER_TOKEN

# EXPECT: 403 Forbidden
# FAIL if: 200 (user role should NOT be allowed to write — all writes are admin-only per confirmed role matrix)
# NOTE: changed from "200 expected" (Phase 9) to "403 expected" (Phase 10 confirmed matrix)
