info:
  name: RBAC11 - viewer GET /api/sites → 200 (read is always allowed)
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:50680/api/sites
  headers:
    - name: Authorization
      value: Bearer {{viewer_token}}
      enabled: true

runtime:
  variables:
    - name: viewer_token
      value: FILL_IN_VIEWER_TOKEN

# EXPECT: 200 OK with JSON array
# Confirms viewer can still read — RBAC only blocks writes
