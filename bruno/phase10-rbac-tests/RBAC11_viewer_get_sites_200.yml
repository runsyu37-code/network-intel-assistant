info:
  name: RBAC11 - viewer GET /api/sites → 200 (read is always allowed)
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:50680/api/sites
  headers:
    - name: Authorization
      value: Bearer {{viewer_token}}

runtime:
  variables:
    - name: viewer_token
      value: FILL_IN_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
