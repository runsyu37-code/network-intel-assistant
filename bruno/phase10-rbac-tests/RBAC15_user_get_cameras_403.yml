info:
  name: RBAC15 - user GET /api/cameras → 403
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:50680/api/cameras
  headers:
    - name: Authorization
      value: Bearer {{user_token}}

runtime:
  variables:
    - name: user_token
      value: FILL_IN_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
