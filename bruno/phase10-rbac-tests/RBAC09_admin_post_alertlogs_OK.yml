info:
  name: RBAC09 - admin POST /api/alert-logs → NOT 403 (admin allowed)
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:50680/api/alert-logs
  headers:
    - name: Authorization
      value: Bearer {{admin_token}}
    - name: Content-Type
      value: application/json

runtime:
  variables:
    - name: admin_token
      value: FILL_IN_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
