info:
  name: RBAC08 - user POST /api/ping-logs → 403 (admin-only)
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:50680/api/ping-logs
  headers:
    - name: Authorization
      value: Bearer {{user_token}}
    - name: Content-Type
      value: application/json

runtime:
  variables:
    - name: user_token
      value: FILL_IN_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
