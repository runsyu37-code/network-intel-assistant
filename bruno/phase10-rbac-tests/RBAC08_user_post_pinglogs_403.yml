info:
  name: RBAC08 - user POST /api/pingLogs → 403 (admin-only)
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:50680/api/pingLogs
  headers:
    - name: Authorization
      value: Bearer {{user_token}}
      enabled: true
    - name: Content-Type
      value: application/json
      enabled: true
  body:
    mode: json
    json: |-
      [{"device_id":1,"device_type":"camera","is_online":true,"latency_ms":1}]

runtime:
  variables:
    - name: user_token
      value: FILL_IN_USER_TOKEN

# EXPECT: 403 Forbidden
