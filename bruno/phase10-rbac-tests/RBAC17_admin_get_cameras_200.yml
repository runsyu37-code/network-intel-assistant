info:
  name: RBAC17 - admin GET /api/cameras → 200
  type: http
  seq: 17

http:
  method: GET
  url: http://localhost:50680/api/cameras
  headers:
    - name: Authorization
      value: Bearer {{admin_token}}
      enabled: true

runtime:
  variables:
    - name: admin_token
      value: FILL_IN_ADMIN_TOKEN

# EXPECT: 200 OK (admin has full read access)
# FAIL if: 403
