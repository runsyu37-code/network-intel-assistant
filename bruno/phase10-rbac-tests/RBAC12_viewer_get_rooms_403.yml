info:
  name: RBAC12 - viewer GET /api/rooms → 403
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:50680/api/rooms
  headers:
    - name: Authorization
      value: Bearer {{viewer_token}}
      enabled: true

runtime:
  variables:
    - name: viewer_token
      value: FILL_IN_VIEWER_TOKEN

# EXPECT: 403 Forbidden
# FAIL if: 200 (viewer should not see room-level data per role matrix)
