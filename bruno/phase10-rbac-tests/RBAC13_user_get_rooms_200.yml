info:
  name: RBAC13 - user GET /api/rooms → 200 (user role allowed)
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:50680/api/rooms
  headers:
    - name: Authorization
      value: Bearer {{user_token}}
      enabled: true

runtime:
  variables:
    - name: user_token
      value: FILL_IN_USER_TOKEN

# EXPECT: 200 OK (user can read rooms/racks per role matrix)
# FAIL if: 403
