info:
  name: RBAC03 - viewer DELETE /api/sites/delete/TEST → 403
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:50680/api/sites/delete/TEST
  headers:
    - name: Authorization
      value: Bearer {{viewer_token}}

runtime:
  variables:
    - name: viewer_token
      value: FILL_IN_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
