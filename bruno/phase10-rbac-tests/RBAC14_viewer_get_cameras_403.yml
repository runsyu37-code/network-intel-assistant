info:
  name: RBAC14 - viewer GET /api/cameras → 403
  type: http
  seq: 14

http:
  method: GET
  url: http://localhost:50680/api/cameras
  headers:
    - name: Authorization
      value: Bearer {{viewer_token}}
      enabled: true

runtime:
  variables:
    - name: viewer_token
      value: FILL_IN_VIEWER_TOKEN

# EXPECT: 403 Forbidden
# FAIL if: 200 (viewer should not see device data)
