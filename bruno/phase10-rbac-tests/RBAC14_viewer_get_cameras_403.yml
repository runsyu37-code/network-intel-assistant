info:
  name: RBAC14 - viewer GET /api/cameras → 403
  type: http
  seq: 14

http:
  method: GET
  url: http://localhost:50680/api/cameras
  headers:
    - name: Authorization
      value: Bearer {{viewer_token}}

runtime:
  variables:
    - name: viewer_token
      value: FILL_IN_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
