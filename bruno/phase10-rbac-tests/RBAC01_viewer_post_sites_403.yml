info:
  name: RBAC01 - viewer POST /api/sites → 403
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:50680/api/sites
  headers:
    - name: Authorization
      value: Bearer {{viewer_token}}
    - name: Content-Type
      value: application/json

runtime:
  variables:
    - name: viewer_token
      value: FILL_IN_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
