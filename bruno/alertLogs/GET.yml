info:
  name: GET
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:50680/api/alert-logs
  headers:
    - name: Authorization
      value: Bearer {{admin_token}}
      enabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
