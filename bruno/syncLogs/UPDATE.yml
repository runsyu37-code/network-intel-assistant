info:
  name: UPDATE
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:50680/api/sync-logs/1
  auth: inherit
  headers:
    - name: Authorization
      value: Bearer {{admin_token}}
      enabled: true
    - name: Content-Type
      value: application/json
      enabled: true
  body:
    mode: json
    json: |-
      {
        "id": 1,
        "device_type": "camera",
        "device_id": "1",
        "synced_by": 1,
        "sync_type": "auto",
        "status": "success",
        "message": "Updated via Bruno test"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
