info:
  name: UPDATE
  type: http
  seq: 3

http:
  method: POST
  url: https://localhost:44342/api/Updaterooms/R001
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
      enabled: true
  body:
    mode: json
    json: |-
      {
        "Room_ID": "R001",
        "Site_ID": "S001",
        "Building_ID": "B001",
        "Floor_ID": "F001",
        "name": "Server Room 1 (Updated)",
        "type": "server",
        "has_nvr": true,
        "has_sw": true,
        "note": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
