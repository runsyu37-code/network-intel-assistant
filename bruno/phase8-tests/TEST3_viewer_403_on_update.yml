info:
  name: TEST3 - Viewer blocked from camera update (403)
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:50680/api/cameras/1
  headers:
    - name: Authorization
      value: Bearer {{viewer_token}}
      enabled: true
    - name: Content-Type
      value: application/json
      enabled: true
  body:
    mode: json
    json: |-
      {
        "device_name": "should-be-blocked"
      }

# SETUP: set viewer_token in Bruno environment (login as viewer account first)
# EXPECT: 403 Forbidden
# PASS if: status 403
# FAIL if: status 200 (viewer was able to update camera)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
