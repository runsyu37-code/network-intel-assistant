info:
  name: TEST2 - English error strings (no garbled Thai)
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:50680/api/cameras
  headers:
    - name: Authorization
      value: Bearer {{admin_token}}
    - name: Content-Type
      value: application/json

runtime:
  variables:
    - name: admin_token
      value: FILL_IN_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
