# Task — Dashboard Page

## Goal
Build a new Dashboard page as the default landing after login.

## Mockup
`open design/input/screens_dashboard.html`

## Route
`/dashboard`
- Update redirect in `App.tsx`: after login → `/dashboard` (instead of `/dashboard/topology`)
- Add "Dashboard" as first item in sidebar nav, icon: `LayoutDashboard` from lucide-react

## Files to Create
| File | Purpose |
|---|---|
| `src/pages/DashboardPage.tsx` | New page component |
| `src/styles/dashboard.css` | New CSS file |

## Data
Mock only — no real API call yet.
